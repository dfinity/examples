[package]
edition = "2021"
name = "timer"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
candid = "0.10"
ic-cdk = "0.20"
# The timers library is required for multiple and periodic timers.
ic-cdk-timers = "1.0"
