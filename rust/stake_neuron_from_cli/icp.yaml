networks:
  - name: local
    mode: managed
    nns: true  # deploys NNS canisters at their mainnet IDs on the local network

canisters:
  - name: backend
    recipe:
      type: "@dfinity/rust@v3.3.0"
