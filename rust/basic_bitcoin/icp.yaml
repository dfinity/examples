canisters:
  - name: backend
    recipe:
      type: "@dfinity/rust@v3.3.0"

networks:
  - name: local
    mode: managed
    image: icp-cli-network-launcher-bitcoin
    port-mapping:
      - 0:4943      # IC gateway (dynamic)
      - 18443:18443 # bitcoind RPC (required for ord)

environments:
  - name: local
    network: local
    init_args:
      backend: "(variant { regtest })"

  - name: ic
    network: ic
    # Deploys to Bitcoin testnet4 using test_key_1 by default.
    # For Bitcoin mainnet, change to "(variant { mainnet })" — the canister
    # automatically switches to key_1 when initialized with the mainnet variant.
    init_args:
      backend: "(variant { testnet })"
