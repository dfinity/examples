name: motoko-calc
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/calc/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
