name: motoko-hello-world
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/hello-world/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
