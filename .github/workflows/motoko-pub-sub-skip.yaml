name: motoko-pub-sub
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/pub-sub/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
