name: motoko-factorial
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/factorial/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
