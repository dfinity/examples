name: rust-defi
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - rust/defi/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
