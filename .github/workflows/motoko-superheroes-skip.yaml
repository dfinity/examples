name: motoko-superheroes
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/superheroes/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
