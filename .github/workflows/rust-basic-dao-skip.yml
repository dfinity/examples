name: rust-basic-dao
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - rust/basic_dao/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
