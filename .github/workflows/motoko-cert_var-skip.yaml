name: motoko-cert-var
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - motoko/cert-var/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
