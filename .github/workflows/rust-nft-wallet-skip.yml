name: rust-nft-wallet
on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - rust/nft-wallet/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Not needed - relevant folder not touched'
