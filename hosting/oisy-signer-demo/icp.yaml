canisters:
  - name: frontend
    recipe:
      type: "@dfinity/static-site@v0.3.1"
      configuration:
        dir: frontend/dist
        build:
          - npm install --prefix frontend
          - npm run build --prefix frontend
