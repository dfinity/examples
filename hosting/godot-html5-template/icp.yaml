canisters:
  - name: frontend
    recipe:
      type: "@dfinity/static-site@v0.3.0"
      configuration:
        dir: dist
        build:
          - mkdir -p dist
          - cp -r frontend/assets/* dist/
          - cp frontend/index.html dist/
