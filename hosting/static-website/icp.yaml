canisters:
  - name: frontend
    recipe:
      type: "@dfinity/asset-canister@v2.1.0"
      configuration:
        dir: dist
        build:
          - mkdir -p dist
          - cp -r frontend/assets/* dist/
          - cp -r frontend/src/* dist/
