type Signature = 
 record {
   message: text;
   signature: blob;
   timestamp: nat64;
 };
service : {
  getMySignatures: () -> (vec Signature) query;
  getMyVerificationKey: () -> (blob);
  signMessage: (message: text) -> (blob);
}
